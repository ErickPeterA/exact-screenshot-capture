# PostgreSQL self-hosted. Never expose this value to the browser.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/jornada_do_empreendedor?sslmode=require

# Optional: protects existing cron endpoints.
CRON_SECRET=replace-with-a-long-random-secret
CRON_SECRET_PREVIOUS=
